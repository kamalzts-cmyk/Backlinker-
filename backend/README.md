# backend

FastAPI application. Empty pending **Phase 1** (see `../PRODUCT_SPEC.md`
§9 and `../docs/ARCHITECTURE.md` §9) — no application code is added
before the real crawler exists, so we don't ship a shell of routes over a
crawler that doesn't work yet.

Target layout is documented in `../docs/ARCHITECTURE.md` §2 (`app/api`,
`app/core`, `app/db`, `app/crawler`, `app/engines`, `app/workers`,
`app/tests`).
