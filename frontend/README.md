# frontend

Next.js application. Empty pending the backend API existing to consume
(see `../docs/ARCHITECTURE.md` §4 and §9) — we do not build UI against
mocked/fake data.

Target route layout is documented in `../docs/ARCHITECTURE.md` §2 and the
module list in `../PRODUCT_SPEC.md` §4.
